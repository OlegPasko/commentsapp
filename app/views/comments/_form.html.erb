<%= form_for(@comment, :html => { :class => 'form-vertical' }) do |f| %>
	<div class="control-group">
		<div class="controls">
			<%= f.text_field :name, size: 20, placeholder: 'Имя', class: '' %>
		</div>
	</div>
	<div class="control-group">
		<div class="controls">
			<%= f.text_area :body, rows: 5, placeholder: 'Комментарий', class: 'input-block-level' %>
		</div>
	</div>
	<%= f.hidden_field :comment_id, placeholder: 'Имя', value: params[:id] ? params[:id] : '0', class: '' %>
	<div class="control-group">
		<div class="controls">
			<%= button_tag(type: 'submit', class: "btn") do %>
				<i class="icon-comment"></i> Написать</a>
			<% end %>
		</div>
	</div>
<% end %>